# openapi-typescript's peer range (typescript ^5.x) lags behind the TypeScript version
# Vite's react-ts template installs (6.x); the codegen CLI works fine against it in
# practice, so relax the peer check rather than downgrading the whole project's TS version.
legacy-peer-deps=true
