## Agent skills

### Issue tracker

Issues live as GitHub issues in `yuliia-hrabovenko/subscription-billing`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
